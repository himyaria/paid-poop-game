# 带薪拉屎玩什么

一个 2D 漫画风寻物网页游戏。打开 `index.html` 即可游玩。

## 部署到 GitHub Pages

1. 在 GitHub 新建一个公开仓库。
2. 上传 `index.html`、`styles.css`、`game.js` 和 `assets/` 文件夹。
3. 进入仓库 `Settings` -> `Pages`。
4. `Source` 选择 `Deploy from a branch`。
5. 分支选择 `main`，目录选择 `/root`，保存。

稍等 1-2 分钟，GitHub 会生成一个公开访问链接。
